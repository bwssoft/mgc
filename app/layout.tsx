import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MGC TECNOLOGIA",
  description: "Generated by BSoft",
  icons: "https://www.mgctecnologia.com/logo/metadata-icon.png",
  openGraph: {
    title: "MGC TECNOLOGIA",
    description: "Generated by BSoft",
    siteName: "MGC TECNOLOGIA",
    url: "https://www.mgctecnologia.com",
    type: "website",
    images: [
      {
        url: "https://www.mgctecnologia.com/logo/metadata-icon.png",
        width: 512,
        height: 512,
        alt: "MGC TECNOLOGIA Logo",
      },
    ],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
